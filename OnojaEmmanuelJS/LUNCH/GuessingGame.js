// Write a function that counts how many times each elements appears in an array and returns an object with element counts



function toCountNumberOfItems(Array){
items = []
itemCount = 0;

	for(let count of array){
	if ( count != count){
		return itemCount.push(count)	
	}
	else count++
		return count
		}


}