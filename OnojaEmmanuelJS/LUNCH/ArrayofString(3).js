/*Write a program that takes an array of strings and returns a new array containing the length of each string*/


function getArrayofString(word){
	newWordLength = []
	for (index = 0; index < word.length; index++){
	word[index].length;
	
	newWordLength.push(word[index].length);
	
	}
				
		return newWordLength;
	
				}
	
		


let word = ['Goat', "Cup", "Mango", "School"]
let result = getArrayofString(word);
console.log(result);

















