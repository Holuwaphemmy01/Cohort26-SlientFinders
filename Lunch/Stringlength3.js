//Write a program that takes an array of string and returns a new array containing the length of each string

let words = ["prayer", "elightenment", "exotic", "destruction", "present"];

let lengths = words.map(word => word.length);

console.log(lengths);
