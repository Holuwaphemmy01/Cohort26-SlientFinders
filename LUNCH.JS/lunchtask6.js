const color = ["red", "green", "blue"];
let colorLengths = [];

for(let count = 0; count < colors.length; count++){
     colorLengths[colors[count]] = colors[count].length;
 }

console.log(colorLengths);