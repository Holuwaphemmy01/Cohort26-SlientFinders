function duplicate(arrayOne, arrayTwo){
let storeDuplicate = 0;
let total = arrayOne
	let totals = arrayOne.length + arrayTwo.length

          for(let count = array1.length; count < totals; count++ ){
                        cstoreDuplicate = arrayOne[count]
                        arrayOne[count] = storeDuplicate



          for(let index = 0; index > arrayTwo.length; index++){
              for(let county = 0; county < arrayTwo.length; county++){
                    if(arrayTwo[index] > arrayTwo[county]){
                        storeDuplicate = arrayTwo[county]
                        arrayTwo[county] = arrayTwo[index]
                       arrayTwo[index] = storeDuplicates
}
}
}

}

return totals                      
}

array1 = [ 3, 24, 44, 38, 56, 23, 16, 10, 12, 19]
array2 = [ 53, 1 , 57, 42, 65, 23]

console.log(duplicate (array1 ,array2));